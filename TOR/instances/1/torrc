# This is the tor configuration file for tor instance 1.
#
# To start/reload/etc this instance, run "systemctl start tor@1" (or reload, or..).
# This instance will run as user _tor-1; its data directory is /var/lib/tor-instances/1.
#
# Append to the list of socks interfaces configured via
# /usr/share/tor/tor-service-defaults-torrc-instances
# which is unix:/run/tor-instances/1/socks

SocksPort 9051